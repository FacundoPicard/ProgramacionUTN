/* maquina de escribir */

/* let text= documemt.getElementBy("textoinicio");
let str= text.innerHTML;

text.innerHTML = "";

let speed= 200;
let i = 0

function maquinaescribir(){
    if(i < str.length){
        text.innerHTML += str.charAt(i);
        i++;
        setTimeout(maquinaescribir, speed);
    }
}

setTimeout(maquinaescribir, speed);
 */